#include "gpio.hpp"

Gpio::Gpio(uint16_t pin, GPIO_TypeDef* port) : mpin(pin), mport(port)
{

}

Gpio::gpio_init()
{
	RCC->AHB1ENR |= RCC_AHB1ENR_GPIODEN;
	mport->MODER |= 1UL << (pin * 2);
}

Gpio::gpio_write(uint8_t pin, uint8_t state)
{
	RCC->AHB1ENR |= RCC_AHB1ENR_GPIODEN;
	mport->MODER |= 1UL << (pin * 2);
}


void gpio_write(uint8_t pin, uint8_t state){
	if(state == 1 || state == 0)
	{
		GPIOD->ODR = state << pin;
	}
}
