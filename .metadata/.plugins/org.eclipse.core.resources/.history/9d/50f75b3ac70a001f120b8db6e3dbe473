#include "app_main.hpp"
#include "gpio.hpp"


void app_main(void)
{
	Gpio greenLed(12, GPIOD);
	greenLed.Init();
	while(true)
	{
		greenLed.Write(1);
		HAL_Delay(1000);
		greenLed.Write(0);
		HAL_Delay(1000);
	}

}
