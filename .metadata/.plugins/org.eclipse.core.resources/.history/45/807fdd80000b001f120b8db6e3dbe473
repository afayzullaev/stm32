#include "app_main.hpp"
#include "gpio.hpp"


void app_main(void)
{
	Gpio userBtn(13, GPIOC, INPUT);
	Gpio greenLed(12, GPIOD, OUTPUT);
	Gpio redLed(13, GPIOD, OUTPUT);
	Gpio blueLed(14, GPIOD, OUTPUT);
	Gpio yellowLed(15, GPIOD, OUTPUT);

	while(true)
	{
		if(userBtn.Read() == HIGH)
		{
			greenLed.Write(HIGH);
			redLed.Write(HIGH);
			blueLed.Write(HIGH);
			yellowLed.Write(HIGH);
		}
		greenLed.Write(LOW);
		redLed.Write(LOW);
		blueLed.Write(LOW);
		yellowLed.Write(LOW);

	}
}
