#include "app_main.hpp"
#include "gpio.hpp"


void app_main(void)
{
	Gpio greenLed(12, GPIOD);
	greenLed.Init();
	while(true)
	{
		greenLed.Write(HIGH);
		HAL_Delay(1000);
		greenLed.Write(LOW);
		HAL_Delay(1000);
	}

}
