#include "gpio.hpp"

Gpio::Gpio(uint16_t pin, GPIO_TypeDef* port) : mpin(pin), mport(port)
{

}

Gpio::Enable_Clock(GPIO_TypeDef* port)
{

}

void Gpio::Init(void)
{
	RCC->AHB1ENR |= RCC_AHB1ENR_GPIODEN;
	mport->MODER |= 1UL << (mpin * 2);
}

void Gpio::Write(PIN_STATE state)
{
	switch (state) {
		case LOW:
			mport->ODR &= ~(1 << mpin);
			break;
		case HIGH:
			mport->ODR |= 1 << mpin;
			break;
		default:
			break;
	}
}

